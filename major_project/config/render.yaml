services:
  - type: web
    name: elective-guidance-system
    env: node
    plan: free
    rootDir: major_project
    buildCommand: npm install --include=dev && npm run build
    startCommand: node simple-server.cjs
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 5000
